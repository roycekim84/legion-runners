# Progress

## 2026-08-23 — Prototype 0.1

### 구현 완료
- Flutter 프로젝트 및 Flame 의존성 추가
- 세로형 고정 해상도(390×844) 게임 캔버스
- 자동 진행 타이머와 좌우 드래그 lane 이동
- 사다리꼴 도로 기반 2.5D 원근 표현
- 실제 병력 수와 표시 캐릭터 수 분리
- +10 / ×2 게이트
- 자동 좀비 전투 및 최종 150 웨이브
- Victory / Defeat 결과와 다음 출정

### 테스트 방법
`flutter analyze`, `flutter build web`, Web에서 드래그 및 두 게이트 버튼을 순서대로 조작한다.

검증 완료: `flutter analyze` 통과, `flutter test` 통과, `flutter build web --release` 성공.

### 남은 문제
- 현재는 임시 도형 렌더링이며 실제 스프라이트·사운드가 없다.
- 게이트/유닛/스테이지 수치가 코드에 임시로 있다.
- 최종 전투의 피격·넉백·공격 애니메이션이 단순하다.

### 다음 작업
검사·궁수·기사 데이터를 추가하고, 전투 역할이 화면에서 구분되도록 한다.

## 2026-08-23 — Prototype 0.2: Initial Run / Gate Polish

### 구현 완료
- 생성형 전장 배경 asset 적용: 성채·석조 다리·청색 게이트·횃불
- 초기 런 화면을 세로형 게임 화면답게 개선
- 인간/좀비 군단 stat card와 진행 bar 추가
- `RUN 01`, `STAGE 1-1`, 일시정지 스타일 HUD 추가
- 게이트 버튼 발광 테두리·그림자·선택 안내 추가
- 배경과 코드 렌더링을 결합해 UI가 게임 화면을 가리지 않도록 조정

### 검증
`flutter analyze`, `flutter test`, `flutter build web --release` 통과.

### 남은 문제
- 캐릭터는 여전히 임시 도형이며 병종별 스프라이트가 없다.
- 모바일 기기에서 실제 터치 감도와 화면 비율을 추가 확인해야 한다.

### 다음 작업
검사·궁수·기사 3종을 같은 아트 스타일의 스프라이트/실루엣으로 추가하고, 게이트 선택에 병종 전략을 연결한다.

## 2026-08-23 — Prototype 0.3: Unit Choice Gates

### 구현 완료
- 민병대·검사·궁수·기사 군단 구성 데이터 추가
- 2차 게이트를 3-way 선택으로 확장: 검사 +30 / 궁수 +10 / 기사 +10
- 병종별 화면 색상과 머리 실루엣 구분
- 병종별 전투 기여도 적용: 궁수 원거리 보정, 기사 정예 전투력 보정
- 인간 군단 HUD에 병종별 보유 수량 chip 추가
- 첫 전투 → 병종 게이트 → 두 번째 전투 → 최종 150 좀비 흐름 고정

### 검증
`flutter analyze`, `flutter test`, `flutter build web --release` 통과.

### 다음 작업
병종별 실제 스프라이트와 공격 이펙트를 추가하고, 궁수 화살·기사 돌진·검사 근접 공격을 시각적으로 분리한다.

## 2026-08-23 — Prototype 0.4: Combat Feedback

### 구현 완료
- 궁수 화살 projectile 효과
- 검사 검격 arc 효과
- 기사 돌진 충격선과 원형 충격파
- 피격 burst 효과
- 전투 tick마다 병종 구성에 따라 효과를 생성
- 효과 수명 관리 및 자동 제거로 장시간 전투 누적 방지

### 검증
`flutter analyze`, `flutter test`, `flutter build web --release` 통과.

### 다음 작업
적 역할 차이와 좀비 피격/넉백 애니메이션을 추가한다.

## 2026-08-23 — Prototype 0.5: Enemy Roles

### 구현 완료
- 일반 좀비, 스켈레톤, 구울, 언데드 기사 타입 추가
- 웨이브별 적 타입 표시: 좀비 → 스켈레톤 → 언데드 기사 혼합 최종군
- 적 타입별 색상·머리 실루엣 차이
- 스켈레톤·구울·언데드 기사별 공격 압박 수치 차이
- 최종 웨이브에서 여러 적 타입이 섞여 보이도록 구성
- HUD에 현재 적 타입 표시

### 검증
`flutter analyze`, `flutter test`, `flutter build web --release` 통과.

### 다음 작업
적별 피격·넉백 애니메이션과 네크로맨서 소환 패턴을 추가한다.

## 2026-08-23 — Prototype 0.6: Enemy Feedback & Summoner

### 구현 완료
- 적 피격 시 군단 전체가 짧게 뒤로 밀리는 넉백 표현
- 넉백 방향선과 피격 충격 효과 추가
- 최종 웨이브에 네크로맨서 실루엣 추가
- 네크로맨서가 5초마다 좀비 15마리를 소환
- 소환 원형 마법진과 HUD 메시지 추가
- 네크로맨서의 낮은 직접 공격 압박으로 소환형 역할 구분

### 검증
`flutter analyze`, `flutter test`, `flutter build web --release` 통과.

### 다음 작업
영웅 1명과 스킬 버튼을 추가해 자동 전투 중 능동적인 한 번의 개입을 제공한다.

## 2026-08-23 — Prototype 0.7: Hero Skill

### 구현 완료
- 인간 기사단장 영웅을 군단 전방에 배치
- 전투 중 우측 하단 `돌격` 스킬 버튼 추가
- 스킬 사용 시 적 대량 피해와 넉백 발생
- 영웅 돌격 라인·충격파 효과 추가
- 8초 쿨다운과 남은 시간 표시
- 게이트/결과 화면에서는 스킬 버튼 비활성화

### 검증
`flutter analyze`, `flutter test`, `flutter build web --release` 통과.

### 다음 작업
스테이지 데이터 분리와 이벤트 선택 화면을 추가해 런마다 선택 결과가 달라지도록 한다.

## 2026-08-23 — Prototype 0.8: Stage Data & Event Choice

### 구현 완료
- `stage_data.dart`로 스테이지·게이트·이벤트 정의 분리
- 두 번째 전투 뒤 `운명의 선택!` 이벤트 화면 추가
- 난민 구출: 민병대 +25
- 버려진 무기고: 검사 +15
- 저주받은 성배: 병력 -10%, 공격력 +40%
- 이벤트 결과를 최종 전투 병력·공격력에 반영
- 이벤트 화면에서 영웅 스킬 버튼 숨김 처리

### 검증
`flutter analyze`, `flutter test`, `flutter build web --release` 통과.

### 다음 작업
결과 화면 보상과 영구 성장 데이터를 연결한다.

## 2026-08-23 — Prototype 0.9: Sprite Pass for Initial Run

### 구현 완료
- ImageGen으로 인간 4종 스프라이트 시트 생성
- ImageGen으로 언데드 5종 스프라이트 시트 생성
- 체크 패턴 제거 및 투명 PNG asset 정리
- Flame에서 병종별 source rect로 스프라이트 렌더링
- 원근에 따라 스프라이트 크기와 군집 간격 조절
- 기존 임시 도형을 asset 로딩 실패 시에만 사용하는 fallback으로 변경
- 초기 런 화면의 실제 캐릭터 가독성 개선

### 검증
`flutter analyze`, `flutter test`, `flutter build web --release` 통과.

### 다음 작업
스프라이트 애니메이션 프레임과 결과 보상 화면을 연결한다.

## 2026-08-23 — Screen 3/4 Directional Combat Correction

### 기획 정정
기존 프로토타입은 캐릭터 방향, 걷기 애니메이션, 실제 전진, projectile 충돌을 충분히 구현하지 않아 Screen 3의 핵심 경험을 충족하지 못했다. 현재 스프라이트는 병종 구분용 임시 시트이며 후면 아군·전면 적 방향 계약을 만족하는 최종 asset이 아니다.

### 최우선 재구축 항목
- 아군 후면 걷기 루프
- 적 전면 걷기 루프
- 양 진영 반대 방향 전진
- 궁수 화살의 실제 이동·충돌
- 검사 검격과 기사 돌진 충돌
- 피격·넉백·쓰러짐 상태
- Screen 4 게이트 선택 직후 진형·공격 방식 변화

### 보류
보상·영구 성장·추가 종족·다수 스테이지는 위 Screen 3/4 완료 판정 이후 진행한다.

## 2026-08-23 — Gate 0.1: Directional Walk Pass

### 구현 완료
- 아군 후면 4행×4프레임 걷기 스프라이트 시트 생성·적용
- 적 전면 5행×4프레임 걷기 스프라이트 시트 생성·적용
- Flame 시간 기반 걷기 프레임 교체
- 플레이어 군단은 아래에서 위로, 적 군단은 위에서 아래로 접근
- 전투 진입·이벤트 선택 시 Formation 접근 거리 초기화
- 기존 병종별 sprite fallback 유지

### 검증
`flutter analyze`, `flutter test`, `flutter build web --release` 통과.

### 남은 Gate 0
실제 projectile 이동·충돌, 검사 근접 판정, 기사 돌진 판정, 피격 상태 머신을 구현해야 한다.
